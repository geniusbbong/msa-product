spring:
  application:
    name: product-price

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka/}

server:
  port: ${PORT:8061}


management:
  endpoints:
    web:
      exposure:
        include:
        - "*"
